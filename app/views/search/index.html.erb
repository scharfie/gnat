<% heading 'Search results' %>
<div id="results">
<% @results.each do |key, results| %>
  <% next if results.empty? %>
  <h3><%= key %> <span>(<%= pluralize results.length, 'result' %>)</span></h3>
  <dl>
    <%= render :partial => 'result', :collection => results %>
  </dl>
<% end %>
</div>