  <tr>
    <td><%= link_to ticket.id, project_ticket_path(ticket) %></td>
    <td><%= ticket.status %></td>
    <td><%= link_to ticket.title, project_ticket_path(ticket) %></td>
    <td><%= time_ago_in_words ticket.created_on %></td>
  </td>
